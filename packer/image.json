{
  "builders": [
    {
      "type":      "yandex",
      "token":     "${YC_TOKEN}",
      "folder_id": "${YC_FOLDER_ID}",
      "zone":      "${YC_ZONE}",

      "image_name":        "${IMG_NAME}-{{isotime | clean_resource_name}}",
      "image_family":      "${IMG_FAMILY}",
      "image_description": "${DESCRIPTION}",

      "source_image_family": "debian-12",
      "subnet_id":           "${SUBNET_ID}",
      "use_ipv4_nat":        true,
      "disk_type":           "network-hdd",
      "ssh_username":        "${USERNAME}",
      "ssh_private_key_file": "/home/anestesia/.ssh/id_ed25519"
    }
  ],
  "provisioners": [
    {
      "type": "shell",
      "inline": [
       
      ]
    }
  ]
}
